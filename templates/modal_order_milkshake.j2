<!-- Modal -->
<div class="modal fade" id="modalOrderMilkshake" tabindex="-1" role="dialog" aria-labelledby="orderMilkshake" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content bg-dark text-white">
      <div class="modal-header">
        <h5 class="modal-title" id="orderMilkshake">Milkshake</h5>
        <button type="button" class="close text-white" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <form method="POST" role="form" novalidate>
          <div class="container">
            <div class = "form-row">
              <div class="form-group col">
                    {{milkshakeForm.hidden_tag()}}

                    <div>
                        {{ milkshakeForm.flavor.label(class="form-control-label") }}
                        {# Adds "is-invalid" bootstrap styling to field if the field has errors #}
                        {{milkshakeForm.flavor(class="form-control" + (" is-invalid" if milkshakeForm.flavor.errors else ""))}}
                        {% if milkshakeForm.flavor.errors %}
                            {% for error in milkshakeForm.flavor.errors %}
                            <div class="invalid-feedback">{{ error }}</div>
                            {% endfor %}
                        {% endif %}
                    </div>
                </div>
                <div class="form-group col">
                    <div>
                        {{ milkshakeForm.quantity.label(class="form-control-label") }}
                        {# Adds "is-invalid" bootstrap styling to field if the field has errors #}
                        {{milkshakeForm.quantity(class="form-control" + (" is-invalid" if milkshakeForm.quantity.errors else ""))}}
                        {% if milkshakeForm.quantity.errors %}
                            {% for error in milkshakeForm.quantity.errors %}
                            <div class="invalid-feedback">{{ error }}</div>
                            {% endfor %}
                        {% endif %}
                    </div>
                </div>
                <div class="form-group col">
                    <div>
                        {{ milkshakeForm.addWhippedCream.label(class="form-control-label") }}
                        {# Adds "is-invalid" bootstrap styling to field if the field has errors #}
                        {{milkshakeForm.addWhippedCream(class="form-control" + (" is-invalid" if milkshakeForm.addWhippedCream.errors else ""))}}
                        {% if milkshakeForm.addWhippedCream.errors %}
                            {% for error in milkshakeForm.addWhippedCream.errors %}
                            <div class="invalid-feedback">{{ error }}</div>
                            {% endfor %}
                        {% endif %}
                    </div>
                </div>
                <div class="form-group col">
                    <div>
                        {{ milkshakeForm.addCherry.label(class="form-control-label") }}
                        {# Adds "is-invalid" bootstrap styling to field if the field has errors #}
                        {{milkshakeForm.addCherry(class="form-control" + (" is-invalid" if milkshakeForm.addCherry.errors else ""))}}
                        {% if milkshakeForm.addCherry.errors %}
                            {% for error in milkshakeForm.addCherry.errors %}
                            <div class="invalid-feedback">{{ error }}</div>
                            {% endfor %}
                        {% endif %}
                    </div>
                </div>
              </div>
            </div>
            <div class="form-row">
              <div class="col-md-4">
                {{milkshakeForm.submitOrderMilkshake()}}
              </div>
              <div class="col-md-3">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
              </div>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
{# Make sure to keep modal up if there are errors preventing adding employee #}
{# {% if registerForm.errors %}
  <script>
    $('#modalRegisterEmployee').modal('show');
  </script>
{% endif %} #}